users > ActionType: define all the actions , convert to URL
eg. GET_USERS --> 'users/getUsers'

users > thunk 


createSlice users used in UserList

reducers createSlice (transform reducers>index.js to use in itemsList)
